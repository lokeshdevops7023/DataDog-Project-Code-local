apiVersion: v1
kind: Pod
metadata:
  name: sanjana-pod
  labels:
    app: nginx
    environment: production
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      ports:
        - containerPort: 80
      env:
        - name: MY_ENV_VAR
          value: "Hello Kubernetes"
      resources:
        requests:
          memory: "64Mi"
          cpu: "250m"
        limits:
          memory: "128Mi"
          cpu: "500m"
